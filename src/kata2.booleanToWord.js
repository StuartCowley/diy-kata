const booleanToWord = (boolean) =>
  //    if (boolean === true) {
  //        return 'Yes';
  //    } else {
  //        return 'No';
  //    }
  boolean ? "Yes" : "No";
// refactored code using ternary operator ?

module.exports = booleanToWord;
